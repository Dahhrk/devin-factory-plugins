{
  "name": "typescript-kit",
  "version": "0.4.0",
  "description": "TypeScript poteto bar: PSR handbook + strict (extends-aware), fetch/URL/env + JSON/DOM rg boundaries, oxlint adoption gate (config + factory rules + live run), runtime/drive proof, product-vs-library .d.ts policy, no unexplained any/assertions/double-assertion, described @ts-expect-error, typecheck aliases, selfcheck fixtures.",
  "skills": "./skills",
  "rules": "./rules"
}
