// Intentional smells for slint-rg-gate discrimination (not product code).
export component Smell inherits Window {
    callback bang();
    TouchArea {
        clicked => {
            debug("leftover hygiene smell");
            root.bang();
        }
    }
}
