# lua-kit (Devin edition)

Twin of `Dahhrk/plug-factory` `lua-kit`. Skills: `lua`, `lua-net`,
`lua-ui`, `poteto-lua`. Rule: `rules/lua.md`. Gate:
`scripts/lua-rg-gate.sh` (product Tier 0).

Edit substance in plug-factory; re-export / mirror here. Draft PRs only.
